#include <limits.h>
#include <vector>
#include <string>
#include <cmath>

int travllingSalesmanProblem(std::vector<std::vector<int>> graph, int s, std::string& ans);

int tsp_greedy(std::vector<std::vector<int>> graph);